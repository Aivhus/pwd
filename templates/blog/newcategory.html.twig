{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="title">Création d'une nouvelle catégorie</h1>

    <div class="table">

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <button class="btn btn-secondary" type="submit">Ajouter une catégorie</button>
    {{ form_end(form) }}
    </div>
{% endblock %}